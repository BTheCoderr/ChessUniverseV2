# MongoDB connection string (required for full app: npm run dev / npm start)
MONGODB_URI=mongodb://localhost:27017/chess-universe

# Session secret for Express sessions
SESSION_SECRET=change-me-to-a-random-string

# Server port (default 3001 for full app)
PORT=3001

# Optional: NODE_ENV=development
